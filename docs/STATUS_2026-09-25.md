# Kobe's Betting Hub status — 2026-09-25

## Verified in this checkout
- Local test suite: 536 passed, 0 failed. The test setup uses local fixtures; it does not establish that Discord, Stripe, Render, or Cloudflare production settings are correct.
- Live public routes `/`, `/join`, `/free-pick`, `/recaps`, `/results`, `/exclusives`, `/guides/`, `/sitemap.xml`, and `/robots.txt` returned HTTP 200. Sitemap lists the principal public pages and guide articles.
- Home page still contains the three historical proof images and links to the verified results page. The results page notes that historical coverage may not include posts before the verified tracker began.
- The served Meta Pixel script fires PageView and InitiateCheckout. It does not fire a verified Purchase event. The checkout worker has a separate Stripe-verified `payment_completed` event for internal analytics.
- Source code has one-time $10 referral reward per qualifying subscription and creator profiles that can exist without purchasing membership. Actual creator production profiles, payouts, and Stripe Connect configuration were not verified here.
- The source workbook contains 100 prospects, 22 rows with public email addresses, and 20 distinct email inboxes. The two duplicate inboxes were held for coordination.

## Prepared locally in this checkout
- Free writeup publishing now emits one Discord message per teaser and removes legacy board posts during reconciliation. It keeps player/team identity out of public text. Focused tests passed.
- Arbitrage startup rejects a destination that does not deny `@everyone` channel access or equals the approval channel. The existing approval click rechecks live odds before publishing. Production posting remains controlled by the Render environment flags; their live values were not verified.
- The shared site stylesheet version is consistent across public pages; the full test suite passes.
- A deduplicated outreach queue, three Meta vertical ad concepts, a Higgsfield production workflow, and one 9:16 static creative draft are ready for review.

## Still requiring live verification or integration
- Render deployment commit, arbitrage feature flag and member-posting flag, live Odds API quota, channel role overwrites, Kobe's approve/send permissions, and an actual approval-to-VIP post. Do not call this production-ready until checked.
- Paid football writeup delivery to the correct VIP channel and its exact full pick content on a recent real post.
- Search Console indexing/sitemap status. The available Arc browser session showed a not-verified Search Console property; this is an account/session access issue, not proof that the site is unverified in Kobe's account.
- Stripe Connect payout and bank setup, Zakai/Kobe 50/50 net-profit accounting, RSK/Sports Centered creator status and temporary VIP access. Draft agreements have not been sent or signed.
- Meta written approval for betting-related advertising, permitted targeting, final ad creative and budget. Implement and validate a confirmed, deduplicated Purchase event before running a conversion campaign.
